/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        remotePatterns: [
            {
                hostname: 'yewtgcfblvdxhrqhzsea.supabase.co',
            },
        ],
    },
};

export default nextConfig;
